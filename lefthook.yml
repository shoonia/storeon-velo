pre-commit:
  parallel: true
  commands:
    lint:
      run:
        - npm run lint
        - npm run build
        - npm run test
        - npm run size
      skip:
        - merge
        - rebase
